# JWT 관련 설정
jwt:
  # 실제 운영 환경에서는 환경 변수나 다른 보안 메커니즘을 통해 주입받는 것이 안전합니다.
  secret: V2VMaXZlSW5BVkNBdGl0eUFkdmFuY2VkQ29tcGFueUFjdGl2ZWx5RGV2ZWxvcGluZ1RTQUZvclRoZUZ1dHVyZQo=
  expiration-time: 3600000 # 1시간


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA 설정 (이 부분을 추가하거나 수정하세요)
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true # 실행되는 SQL 쿼리를 로그로 보여줍니다.
    properties:
      hibernate:
        format_sql: true